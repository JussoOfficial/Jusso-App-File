import 'package:flutter/material.dart';

class JColors {
  // Private Constructor
  JColors._();

  // App Basic Colors
  static const Color primaryColor = Colors.deepOrange;
  static const Color accentColor = Colors.white;

  // Text Colors
  static const Color textColor = Colors.deepOrange;
  static const Color textColorSecondary = Colors.black38;
  static const Color textColorTertiary = Colors.grey;
  static const Color textColorQuaternary = Color.fromARGB(255, 10, 127, 222);

  // Background Colors
  static const Color backgroundColor = Colors.white;
  static const Color scaffoldBackgroundColor = Colors.white;

  // Validation Colors
  static const Color errorColor = Color.fromARGB(255, 233, 22, 7);
  static const Color successColor = Color.fromARGB(255, 17, 246, 25);

  // Neutral Colors
  static const Color transparent = Colors.transparent;
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}
